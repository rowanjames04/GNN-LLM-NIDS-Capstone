# Runtime dependencies. Python 3.13, plain venv.
#
# Kept deliberately tight: an unused dependency is a claim the project makes
# about itself that is not true, and several here were dropped when the decision
# that motivated them was reversed.

# Core numerics / data
numpy>=2.0
pandas>=2.2
pyarrow>=17.0          # Parquet; 10-20x faster loads than CSV at 2.4M+ rows

# Classical ML + baselines
scikit-learn>=1.5
xgboost>=2.1           # the strongest topology-blind comparator

# Deep learning
torch>=2.6
torch-geometric>=2.6
# NOTE: torch and xgboost cannot be imported into one process on macOS -- both
# bundle an OpenMP runtime and the process segfaults (exit 139). Tree and torch
# models run as separate invocations.

# Figures
matplotlib>=3.9

# LLM clients. Each has an adapter in src/gnnids/llm/adapters.py and an entry
# in the roster in configs/llm.yaml.
anthropic>=0.40
openai>=1.50
google-genai>=1.0      # Gemini; client.models.generate_content API
ollama>=0.4

# UI (Phase 8 replay demo)
streamlit>=1.39
plotly>=5.24           # not yet used -- for the pending topology view
pyvis>=0.3.2           # not yet used -- for the pending topology view

# Config / CLI / secrets
pyyaml>=6.0
python-dotenv>=1.0     # API keys live in .env, git-ignored

# Dev
pytest>=8.3
ruff>=0.7

# ---------------------------------------------------------------------------
# Removed, with the reason, so they are not reinstated by habit:
#
#   captum            integrated gradients is implemented directly in
#                     src/gnnids/explain/attribution.py -- attributing over
#                     edge_attr needs a custom forward wrapper anyway, and the
#                     method has to be defensible in the report
#   imbalanced-learn  SMOTE was rejected: it interpolates feature vectors, which
#                     is undefined for graph topology
#   statsmodels       Granger causality is out of scope -- neither dataset
#                     carries a timestamp column
#   networkx          torch-geometric covers the graph handling actually used
#   scipy             transitive via scikit-learn; never imported directly
#   typer, rich, tqdm never used -- the scripts are argparse CLIs
